{
  "name": "slippers",
  "version": "0.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "build:lib": "tsc",
    "build:examples": "coffee --compile --bare --output build/examples/ examples/*.coffee",
    "build": "npm run build:lib && npm run build:examples",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "coffeescript": "^2.5.1",
    "typescript": "^4.0.5"
  },
  "dependencies": {}
}
